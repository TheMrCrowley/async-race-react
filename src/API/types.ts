export interface CarData {
  name: string;
  color: string;
  id: number;
}

export interface EngineResponse {
  velocity: string;
  distance: string;
}

export type DriveResponse = { success: boolean };
