export enum GarageSettings {
  PAGE = 1,
  LIMIT = 7,
}

export enum EngineStatus {
  START = 'started',
  STOP = 'stopped',
  DRIVE = 'drive',
}

export enum APIUrls {
  CARS = 'http://127.0.0.1:3000/garage',
  ENGINE = 'http://127.0.0.1:3000/engine',
}
